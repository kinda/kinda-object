"use strict";

var assert = require('chai').assert;
var EventManager = require('./event-manager');
var KindaObject = require('./');

suite('EventManager', function() {
  var foo = EventManager.instantiate();

  test('beginEventSession', function() {

  });
});
